

// ////////          process method
process.cwd();     // current working directory


//
// npm install <-package_name->   // to install package
// npm install 
// npm init     // to create package




// ///// global import //////

//sudo chown -R $USER/usr/local/lib/node_modules    // This command is written because we need admin access before installing globally.

// npm install -g <-package_name->  // for global install

// ///////       even if we install package globaly then after also we have to link package

// npm install -g <-package_name->

// npm link <-package_name->  // this will come from our system, not online

//         we should not install package globaly   /////////////////

